services:
  portfolio:
    image: nginx:alpine
    container_name: portfolio
    restart: unless-stopped
    ports:
      - 8080:80
    volumes:
      - ./website:/usr/share/nginx/html:ro

    networks:
      - proxy

networks:
  proxy:
    external: true
